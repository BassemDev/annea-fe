export interface IndicatorEditorRouteParam {
  id: string;
}

export interface FormValues {
  indicator?: number | null;
  variable?: number | null;
  turbineId?: number | null;
  timestamp?: Date | null;
}
